<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileNotWorkGame"
        tools:targetApi="31" >
        <activity
            android:name=".RegistrierungActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MobileNotWorkGame.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/menu_main"
            android:theme="@style/Theme.MobileNotWorkGame.NoActionBar">
        </activity>
        <activity
            android:name=".AdminOberflaecheActivity"
            android:exported="true"
            android:label="@string/activity_admin_oberfläche"
            android:theme="@style/Theme.MobileNotWorkGame.NoActionBar">
        </activity>

        <activity
            android:name=".MetaData"
            android:exported="true"
            android:label="@string/activity_admin_oberfläche"
            android:theme="@style/Theme.MobileNotWorkGame.NoActionBar">

        </activity>
    </application>

</manifest>