import MapWrapper from "../components/map/MapWrapper";
import Box from "@mui/material/Box";

const MapView: React.FC = () => {
  
  return (
    <Box>  
      <MapWrapper width="100vw" height="100vh" />
    </Box>
  );
};

export default MapView;